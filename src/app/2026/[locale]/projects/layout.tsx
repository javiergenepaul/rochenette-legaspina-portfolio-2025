import { Metadata } from "next";
export const metadata: Metadata = { title: "Rochenette Legaspina 2026 - Projects" };
export default function ProjectsLayout2026({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
